import React from 'react'

const ContainerCarrousel = ({ children }) => {
    return (

        <div className='container_carrousel'>
            {children}
        </div>

    )
}

export default ContainerCarrousel